import React, { FC } from 'react';

interface IProductDetailProps {
  
};

const ProductDetail:FC<IProductDetailProps> = ({}) => {
  
  return(
    <div>ProdutDetail</div>
  )
};

export default ProductDetail;